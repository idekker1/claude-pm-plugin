Bootstrap the Claude PM Plugin structure in this project using the setup skill.

Creates missing folders (jobs/, issues/), template files (ROADMAP.md, REFERENCE.md,
WORKFLOW.md, plugin-config.yaml), and the GitHub PR template. Safe to run multiple
times — never overwrites existing files.

$ARGUMENTS may be:
- Empty: run full setup check and create all missing structure
